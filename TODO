Todo:
1) Get the CompoundLayoutContainer working correctly with DynamicLayouts and PixelLayouts that contain a DynamicLayout.
2) Move the CompoundLayoutContainer into it's own file.
3) Remove the runtime channel traits as they just convolute things.
